# done/pending per story — bigpowers cockpit
# Updated: 2026-06-28 (e08 REGISTERED: agent behavioral integration)
type: feat
context: infra
updated: "2026-06-18"

epics:
  e01:                       # Installer — verified DONE (v0 in repo)
    status: done
    stories: { e01s01: done, e01s02: done, e01s03: done }
  e02:                       # Dispatcher — DONE (bin/bts, 8/8 verifies green, shellcheck+shfmt clean)
    status: done
    stories: { e02s01: done, e02s02: done }
  e03:                       # init — DONE (project-local; delegates hooks to sqz/rtk init per d09)
    status: done
    stories: { e03s01: done, e03s02: done }
  e04:                       # verbs — DONE (find/src/docs/ai; docs = context7 MCP launcher per d08)
    status: done
    stories: { e04s01: done, e04s02: done }
  e05:                       # bts map (Rust) — DONE: 10 langs + tiktoken budget + renderer + snapshot (13/13)
    status: done
    stories: { e05s01: done, e05s02: done, e05s03: done }
    pre_publish_hygiene:     # Addressed 2026-06-17
      provenance_fix: done   # Divergent .scm moved to queries/ dir, vendor/queries/ is verbatim
      snapshot_fail: done    # expected_snapshot.txt absent -> test FAILS with clear instructions
      swift_fork: done       # Tracked with deny.toml + SWIFT-FORK-TRACKING comments (blocked on ts 0.25)
  e06:
    status: ready
    stories: { e06s01: done, e06s02: done, e06s03: done }
  e07:
    status: done
    stories: { e07s01: done }
    artifacts:
      - bin/bts (bts config, bts compress, bts map, bts map feedback)
      - crates/bts-map/src/feedback.rs (FeedbackDb, regret-learning score adjustments)
      - crates/bts-map/src/main.rs (main loop: walkdir + feedback wired)
      - crates/bts-map/src/graph.rs (rank_files accepts Option<&FeedbackDb>)
      - crates/bts-map/Cargo.toml (walkdir, serde, serde_json deps)
  e08:                       # Agent behavioral integration — COMPLETE 2026-06-28
    status: done
    stories: { e08s01: done, e08s02: done, e08s03: done, e08s04: done, e08s05: done }

summary:
  total_stories: 21
  done: 21
  pending: 0
  backlog: 0

# v0.3.0 — e07 COMPLETE (npm distribution shipped).
# e08 — agent behavioral integration (REGISTERED 2026-06-28):
#   1. rtk wrapper scripts in ~/.local/bin/ (two-tier: scripts + aliases)
#   2. PATH ordering guard in .envrc
#   3. bts wire hard-gate at line 1 + decision table + new sentinel
#   4. bts find TTY detection + bts grep verb
#   5. bts doctor actionable per-tool fix commands
next: plan-release  # all epics done; next milestone = v0.4.0 release-branch
